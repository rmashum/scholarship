const router = require("express").Router();
const CommunicationController = require("../controller/CommunicationController");
//const auth = require("../middleware/auth");
//const authAdmin = require("../middleware/authAdmin");

//router.post("/delete", auth, authAdmin, userCtrl.deleteUser);

//router.get("/infor", auth, userCtrl.getUser);
//router.get("/history", auth, userCtrl.history);
//router.get("/get", CommunicationController.getGrievance);
//router.post("/save", CommunicationController.addGrievance);
//router.post("/upload",CommunicationController.imageUpload)

module.exports = router;